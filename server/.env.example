# ===== Round Midnight Server =====

# [선택] 서버 포트 (기본값: 3000)
PORT=3000

# [필수 - 프로덕션] 허용할 클라이언트 origin (쉼표 구분)
# 미설정 시 localhost만 허용 (개발용)
# 프로덕션에서는 반드시 Vercel URL 설정 필요 (CORS)
CLIENT_URL=https://your-app.vercel.app

# [필수] Claude API 키 (LLM 연동)
# 미설정 시 하드코딩 폴백 데이터로 동작
ANTHROPIC_API_KEY=your-api-key-here

# ===== 배포 체인 =====
# 1. 클라이언트를 Vercel에 배포 → URL 확인 (예: https://round-midnight.vercel.app)
# 2. CLIENT_URL에 Vercel URL 설정
# 3. 클라이언트의 VITE_SERVER_URL에 이 서버 URL 설정
